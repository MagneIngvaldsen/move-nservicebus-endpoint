# move-nservicebus-endpoint

Console app created to change the endpoints stored for nservicebus in ravendb. 

Note, the migrator for the saga data changes all documents with a property named "Originator"

